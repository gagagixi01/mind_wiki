# Public site reads only approved static content.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Local-only workbench credentials and state.
# Copy to .env.local for local use. Never commit real API credentials.
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=
CURATION_STATE_DIR=.curation
